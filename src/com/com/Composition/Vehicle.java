package com.com.Composition;

// INHERITENCE EXAMPLE (IS A)
public class Vehicle {
    // Composition is similar in concept to inheritence, but it focuses on a "has a" relationship.
    private String name;

    public Vehicle(String name) {
        this.name = name;
    }
}
